spring:
  datasource:
    url: jdbc:h2:mem:tourplanner;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

app:
  open-route-service:
    api-key:
    base-url: https://example.invalid
