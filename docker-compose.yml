services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: tourplanner
      POSTGRES_USER: tourplanner
      POSTGRES_PASSWORD: tourplanner
    ports:
      - "5432:5432"
    volumes:
      - tourplanner-postgres:/var/lib/postgresql/data

volumes:
  tourplanner-postgres:
